---
  interface Props {
    name: string,
    imgSrc: string
  }

  const {name, imgSrc} = Astro.props

---


<article class="w-full">
  <a href="/categorias">
    <figure class="flex items-center max-w-[180px] mx-auto">
      <img src={imgSrc} alt={`Categoria-${name}`} class="w-full h-full object-cover mr-4">
      <figcaption>{name}</figcaption>
    </figure>
  </a>
</article>

